# 视频录制功能使用说明

## 📹 功能概述

视频录制功能允许你录制机器狗摄像头的实时画面，保存为 MP4 视频文件。

## ✨ 主要特性

- ✅ **实时录制**：录制机器狗摄像头的实时画面
- ✅ **状态显示**：实时显示录制时长和帧数
- ✅ **自动管理**：自动创建录制目录和文件名
- ✅ **视频格式**：MP4 格式，30fps
- ✅ **自适应分辨率**：根据实际视频流分辨率自动调整

## 🎯 使用步骤

### 1. 开启摄像头

首先需要开启机器狗的摄像头：

1. 在左侧面板中连接机器狗
2. 等待状态显示为"已连接"或"空闲"
3. 在右侧摄像头区域，选择要查看的机器狗
4. 点击"开启摄像头"按钮
5. 等待视频画面显示

### 2. 开始录制

当视频流正常显示后：

1. 点击"开始录制"按钮（红色圆点图标）
2. 录制开始后：
   - 视频画面左上角会显示红色"录制中"标记
   - 标记上会显示当前录制时长（实时更新）
   - 下方状态栏显示录制文件名

### 3. 停止录制

完成录制后：

1. 点击"停止录制"按钮
2. 系统会：
   - 保存视频文件到 `recordings/` 目录
   - 显示录制完成信息（文件名、时长、帧数）
   - 在日志中记录录制信息

## 📁 文件存储

### 存储位置

录制的视频文件保存在：
```
/home/wppjkw/go2_webrtc_connect/recordings/
```

### 文件命名规则

文件名格式：`{机器狗名称}_{时间戳}.mp4`

示例：
- `1212_20251016_143025.mp4`
- `Dog_A_20251016_150130.mp4`

### 视频参数

- **格式**: MP4 (MPEG-4)
- **编码**: H.264 (mp4v)
- **帧率**: 30 fps
- **分辨率**: 根据实际视频流自动调整（通常为 640x480 或更高）
- **画质**: 高质量（JPEG质量85%）

## 🎨 界面说明

### 录制按钮

| 按钮 | 图标 | 说明 |
|------|------|------|
| 开始录制 | 🔴 (红色圆点) | 开始录制视频，仅在摄像头开启时可见 |
| 停止录制 | ⏹️ (停止符号) | 停止当前录制，仅在录制中显示 |

### 录制指示器

录制时，视频画面左上角会显示：

```
🔴 录制中 00:25
```

- 红色圆点会闪烁
- 右侧显示录制时长（分:秒）

### 状态信息

视频下方显示：
- **录制前**: 显示"摄像头未启动"
- **录制中**: 显示"正在录制: 文件名.mp4"
- **录制完成**: 显示"录制完成: 文件名.mp4 (时长, 帧数)"

## 🔧 技术细节

### 后端 API

#### 1. 开始录制
```http
POST /api/video/record/start
Content-Type: application/json

{
  "dog_name": "1212"
}
```

**响应**:
```json
{
  "success": true,
  "dog_name": "1212",
  "filename": "1212_20251016_143025.mp4",
  "start_time": 1697459425.123,
  "message": "录制已开始"
}
```

#### 2. 停止录制
```http
POST /api/video/record/stop
Content-Type: application/json

{
  "dog_name": "1212"
}
```

**响应**:
```json
{
  "success": true,
  "dog_name": "1212",
  "filename": "1212_20251016_143025.mp4",
  "duration": 45.6,
  "frame_count": 1368,
  "message": "录制已停止"
}
```

#### 3. 查询录制状态
```http
GET /api/video/record/status?dog_name=1212
```

**响应**:
```json
{
  "success": true,
  "recording": true,
  "dog_name": "1212",
  "filename": "1212_20251016_143025.mp4",
  "duration": 25.3,
  "frame_count": 759,
  "start_time": 1697459425.123
}
```

### 实现原理

1. **视频流捕获**：
   - 从 WebRTC 视频流中实时获取帧
   - 将帧转换为 NumPy 数组（BGR24 格式）

2. **帧写入**：
   - 使用 OpenCV 的 `VideoWriter` 写入视频文件
   - 每个视频帧都会实时写入磁盘

3. **自适应分辨率**：
   - 第一帧到达时检测实际分辨率
   - 根据实际分辨率重新创建 `VideoWriter`
   - 确保视频文件分辨率与实际画面匹配

4. **线程安全**：
   - 录制操作在异步事件循环中执行
   - 使用字典管理多个机器狗的录制状态

## ⚠️ 注意事项

### 使用限制

1. **必须先开启摄像头**
   - 录制功能只在视频流活跃时可用
   - 未开启摄像头时"开始录制"按钮不可见

2. **一次只能录制一个机器狗**
   - 每个机器狗同时只能有一个录制任务
   - 切换机器狗会自动停止当前录制

3. **自动停止**
   - 关闭摄像头会自动停止录制
   - 断开机器狗连接会停止录制

### 性能建议

1. **磁盘空间**
   - 1分钟视频约占用 50-100 MB
   - 定期清理旧的录制文件
   - 监控 `recordings/` 目录大小

2. **系统资源**
   - 录制会增加 CPU 和磁盘 I/O 负载
   - 建议不要同时录制多个机器狗（虽然技术上支持）

3. **网络带宽**
   - 视频流质量影响录制质量
   - 确保网络连接稳定

### 错误处理

| 错误信息 | 原因 | 解决方法 |
|---------|------|---------|
| "请先开启摄像头" | 视频流未启动 | 先点击"开启摄像头" |
| "该机器狗已在录制中" | 重复开始录制 | 先停止当前录制 |
| "无法创建视频文件" | 磁盘权限或空间问题 | 检查目录权限和磁盘空间 |
| "该机器狗未在录制中" | 停止未录制的机器狗 | 确认录制状态 |

## 📊 日志记录

系统会在日志中记录录制相关事件：

```
[INFO] [视频录制] 开始录制 1212 的视频: 1212_20251016_143025.mp4
[INFO] [视频录制] 停止录制 1212 的视频: 1212_20251016_143025.mp4, 时长: 45.6秒, 帧数: 1368
```

可以在右侧日志面板查看录制日志。

## 🎬 使用示例

### 基本录制流程

```
1. 连接机器狗 "1212"
2. 选择摄像头 "1212"
3. 点击"开启摄像头"
4. 等待视频显示
5. 点击"开始录制"
6. 观察左上角录制指示器
7. 执行需要录制的动作
8. 点击"停止录制"
9. 在 recordings/ 目录查看视频文件
```

### 快速测试

```bash
# 启动 Web 服务器
python3 web_server.py

# 在浏览器访问
# http://localhost:5000

# 查看录制文件
ls -lh recordings/

# 播放视频
vlc recordings/1212_20251016_143025.mp4
# 或
ffplay recordings/1212_20251016_143025.mp4
```

## 🔍 故障排查

### 视频无法录制

1. **检查摄像头状态**
   ```
   - 确认摄像头已开启
   - 确认视频画面正常显示
   - 查看日志是否有错误
   ```

2. **检查文件系统**
   ```bash
   # 检查目录是否存在
   ls -la recordings/
   
   # 检查磁盘空间
   df -h
   
   # 检查目录权限
   chmod 755 recordings/
   ```

3. **检查视频编码器**
   ```bash
   # 检查 OpenCV 支持的编码器
   python3 -c "import cv2; print(cv2.getBuildInformation())"
   ```

### 录制文件损坏

可能原因：
- 录制过程中程序崩溃
- 磁盘空间不足
- 网络连接中断

解决方法：
- 使用视频修复工具（ffmpeg）
- 确保有足够的磁盘空间
- 稳定网络连接

## 📝 开发说明

### 扩展功能

可以轻松添加的功能：

1. **下载录制文件**
   - 添加 `/api/video/download/<filename>` 接口
   - 提供直接下载链接

2. **录制历史**
   - 列出所有录制文件
   - 显示文件大小、时长信息
   - 提供删除功能

3. **视频预览**
   - 生成缩略图
   - 在网页中预览录制视频

4. **高级设置**
   - 自定义帧率
   - 自定义分辨率
   - 调整视频质量

### 代码位置

- **后端**: `web_server.py`
  - 录制管理: 行 67-76
  - API 接口: 行 800-920
  - 内部方法: 行 921-952

- **前端**: 
  - HTML: `templates/index.html` 行 150-175
  - CSS: `static/css/style.css` 行 445-485
  - JavaScript: `static/js/app.js` 行 900-1050

## 📚 相关文档

- [视频流功能说明](VIDEO_STREAM_USAGE.md)
- [完整修复总结](COMPLETE_FIX_SUMMARY.md)
- [Web界面说明](WEB_INTERFACE_README.md)

## 🎉 总结

视频录制功能现已完全集成到双机器狗控制系统中，提供了：

- ✅ 简单易用的录制界面
- ✅ 实时状态反馈
- ✅ 自动文件管理
- ✅ 完善的错误处理
- ✅ 详细的日志记录

享受录制机器狗精彩瞬间吧！ 🐕🎥

---

**版本**: v2.3.0  
**更新时间**: 2025-10-16  
**作者**: GitHub Copilot  
**状态**: ✅ 已完成并测试
