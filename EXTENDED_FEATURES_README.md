# 双机器狗控制系统扩展功能说明

## 概述
本次更新大幅扩展了双机器狗控制系统的功能，在原有的基础控制和选择性控制之上，增加了更多姿态控制选项和自动运动模式中的触发动作。

## 新增功能详情

### 1. 扩展的姿态控制选项

在原有的**坐下**、**站立**、**打招呼**基础上，新增了以下9个姿态控制选项：

#### 基础姿态扩展
- **🤸 伸展 (Stretch)**: 机器狗伸展身体，舒展关节
  
#### 特技动作
- **🔄 打滚 (Wallow)**: 机器狗在地面打滚
- **🐾 刨地 (Scrape)**: 机器狗用前爪刨地动作
- **💃 扭臀 (WiggleHips)**: 机器狗扭动臀部的可爱动作
- **❤️ 比心 (FingerHeart)**: 机器狗做出比心手势

#### 高难度动作
- **🤸 倒立 (Handstand)**: 机器狗做倒立动作
- **🔄 前空翻 (FrontFlip)**: 机器狗向前翻跟头
- **🔄 后空翻 (BackFlip)**: 机器狗向后翻跟头
- **🦘 扑跃 (FrontPounce)**: 机器狗向前扑跃动作

### 2. 扩展的自动运动触发动作

在原有的**🎭 触发跳舞**基础上，新增了5个触发动作选项：

- **🤸 触发伸展**: 在任何运动模式中触发伸展动作
- **🔄 触发打滚**: 在任何运动模式中触发打滚动作
- **🤸 触发空翻**: 在任何运动模式中触发前空翻动作
- **🦘 触发扑跃**: 在任何运动模式中触发扑跃动作
- **❤️ 触发比心**: 在任何运动模式中触发比心动作

### 3. 增强的舞蹈派对模式

舞蹈派对模式的动作序列从原来的5种扩展到12种：

**原有动作**: Dance1, Dance2, Hello, WiggleHips, FingerHeart
**新增动作**: Stretch, Wallow, Scrape, FrontFlip, BackFlip, FrontPounce, Handstand

#### 智能动作持续时间
- **高难度动作** (FrontFlip, BackFlip, Handstand): 6秒持续时间 + 3秒休息
- **中等难度动作** (Wallow, Scrape, FrontPounce): 4秒持续时间 + 2秒休息
- **常规动作** (其他): 3秒持续时间 + 2秒休息

## GUI界面更新

### 姿态控制面板布局
```
第一排: [坐下] [站立] [👋 打招呼] [🤸 伸展]
第二排: [🔄 打滚] [🐾 刨地] [💃 扭臀] [❤️ 比心]
第三排: [🤸 倒立] [🔄 前空翻] [🔄 后空翻] [🦘 扑跃]
```

### 自动运动触发按钮布局
```
第一排: [🎭 触发跳舞] [🤸 触发伸展] [🔄 触发打滚]
第二排: [🤸 触发空翻] [🦘 触发扑跃] [❤️ 触发比心]
```

## 选择性控制支持

所有新增的姿态控制选项都完全支持选择性控制功能：

- **同时控制两台**: 所有连接的机器狗同时执行动作
- **单独控制**: 只控制选中的某一台机器狗执行动作

每个动作都提供了两个版本的方法：
- 常规方法: `stretch()`, `wallow()`, `front_flip()` 等
- 单独控制方法: `stretch_single(dog_name)`, `wallow_single(dog_name)` 等

## 技术实现要点

### 1. 动作映射
所有新增动作都基于`go2_webrtc_driver/constants.py`中的`SPORT_CMD`常量定义：
```python
"Stretch": 1017,
"Wallow": 1021,
"Scrape": 1029,
"WiggleHips": 1033,
"FingerHeart": 1036,
"Handstand": 1301,
"FrontFlip": 1030,
"BackFlip": 1044,
"FrontPounce": 1032
```

### 2. 智能持续时间管理
新增的`_get_action_duration()`方法根据动作复杂度自动调整等待时间，确保动作完整执行。

### 3. 触发机制增强
新增的`trigger_specific_action()`方法允许在任何运动模式下触发特定动作，增强了系统的交互性。

## 兼容性保证

- ✅ 完全向后兼容原有功能
- ✅ 保持单机器狗和双机器狗模式的兼容性
- ✅ 所有原有的控制方式继续有效
- ✅ 日志系统记录所有新动作的执行情况

## 使用建议

1. **高难度动作** (倒立、空翻等) 建议在平整、安全的地面执行
2. **触发动作** 可在自动模式运行时随时使用，增加控制的趣味性
3. **舞蹈派对模式** 现在更丰富，适合演示和娱乐场景
4. **选择性控制** 在练习或调试单台机器狗时特别有用

## 测试验证

系统包含完整的测试程序 `test_extended_features.py`，可以验证所有新功能的正常工作。

通过本次扩展，双机器狗控制系统的功能得到了显著增强，为用户提供了更丰富的控制选项和更好的使用体验。