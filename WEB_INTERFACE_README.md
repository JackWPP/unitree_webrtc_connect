# 双机器狗Web控制界面

一个现代化的Web控制界面，用于远程控制双机器狗系统。支持跨平台访问，特别适合WSL环境使用。

## ✨ 功能特点

### 🎯 核心功能
- **现代化Web界面** - 基于Bootstrap 5的响应式设计
- **实时状态更新** - 通过WebSocket实时推送机器狗状态
- **跨平台控制** - 在Windows浏览器中控制WSL中的机器狗
- **多控制模式** - 支持同时控制和单独控制
- **完整API支持** - RESTful API + WebSocket实时通信

### 🚀 主要界面功能

#### 1. 连接管理
- **机器狗状态显示** - 实时显示每台机器狗的连接状态
- **一键连接/断开** - 支持批量连接和断开操作
- **动态添加机器狗** - 在线添加和移除机器狗配置
- **连接方式选择** - 支持LocalSTA、LocalAP、Remote三种连接方式

#### 2. 控制模式
- **同时控制模式** - 一个指令控制所有机器狗
- **单独控制模式** - 选择特定机器狗进行控制
- **实时模式切换** - 无需重启即可切换控制模式

#### 3. 自动运动模式
- **正方形走路** - 自动正方形巡逻模式
- **舞蹈派对** - 包含多种动作的舞蹈序列
- **扑跃控制** - 单次扑跃和三次连续扑跃
- **紧急停止** - 一键紧急停止所有运动

#### 4. 手动遥控
- **方向控制** - 前进、后退、左移、右移
- **旋转控制** - 左转、右转
- **键盘快捷键** - WASD方向键，QE旋转，空格停止，ESC紧急停止

#### 5. 姿态控制
- **基础姿态** - 坐下、站立、打招呼、伸展
- **特技动作** - 打滚、刨地、扭臀、比心
- **高难度动作** - 倒立、前空翻、后空翻、扑跃

## 🛠️ 技术架构

### 后端技术栈
- **Flask** - Web框架
- **Flask-SocketIO** - WebSocket实时通信
- **eventlet** - 异步事件处理
- **asyncio** - 异步编程支持

### 前端技术栈
- **Bootstrap 5** - UI框架
- **Font Awesome** - 图标库
- **Socket.IO** - 实时通信客户端
- **原生JavaScript** - 应用逻辑

### 架构特点
- **前后端分离** - REST API + WebSocket
- **实时通信** - 状态变化实时推送
- **响应式设计** - 适配桌面和移动设备
- **模块化设计** - 易于扩展和维护

## 📦 安装和使用

### 1. 安装依赖
```bash
# 安装Web界面依赖
pip install -r web_requirements.txt
```

### 2. 启动Web服务器
```bash
# 使用启动脚本（推荐）
./start_web_server.sh

# 或者直接运行
python web_server.py --host 0.0.0.0 --port 5000
```

### 3. 访问Web界面

#### WSL环境下的访问方式：

1. **获取WSL IP地址**
   ```bash
   # 在WSL中执行
   ip addr show eth0 | grep inet | awk '{print $2}' | cut -d/ -f1
   ```

2. **在Windows浏览器中访问**
   ```
   http://[WSL_IP]:5000
   ```
   例如：`http://172.24.32.45:5000`

3. **本地访问**（在WSL内）
   ```
   http://localhost:5000
   ```

### 4. 配置机器狗

1. **打开Web界面**
2. **添加机器狗配置**
   - 输入机器狗名称（如：Dog1）
   - 输入IP地址（如：192.168.31.245）
   - 选择连接方式（通常选择LocalSTA）
   - 点击"添加机器狗"

3. **连接机器狗**
   - 点击"连接所有机器狗"
   - 等待连接状态变为"已连接"

## 🎮 使用指南

### 基本操作流程

1. **启动系统**
   ```bash
   ./start_web_server.sh
   ```

2. **添加机器狗**
   - 在左侧"机器狗管理"区域添加机器狗配置
   - 确认IP地址正确

3. **建立连接**
   - 点击"连接所有机器狗"
   - 观察连接状态变为绿色

4. **选择控制模式**
   - 同时控制：控制所有机器狗
   - 单独控制：选择特定机器狗

5. **开始控制**
   - 使用自动运动模式进行巡逻或舞蹈
   - 使用手动遥控进行精确控制
   - 使用姿态控制执行特定动作

### 键盘快捷键

| 按键 | 功能 |
|------|------|
| W | 前进 |
| S | 后退 |
| A | 左移 |
| D | 右移 |
| Q | 左转 |
| E | 右转 |
| 空格 | 停止移动 |
| ESC | 紧急停止 |

### 实时状态监控

Web界面提供以下实时状态信息：
- **连接状态** - 每台机器狗的连接状态
- **运动状态** - 当前运动模式和执行信息
- **控制模式** - 当前的控制模式设置
- **错误信息** - 连接或操作错误的实时提示

## 🔧 API 接口

### REST API

#### 机器狗管理
- `GET /api/dogs` - 获取所有机器狗状态
- `POST /api/dogs` - 添加机器狗
- `DELETE /api/dogs/<dog_name>` - 移除机器狗

#### 连接控制
- `POST /api/dogs/connect` - 连接机器狗
- `POST /api/dogs/disconnect` - 断开连接

#### 运动控制
- `POST /api/movement/start` - 启动运动模式
- `POST /api/movement/stop` - 停止运动
- `POST /api/movement/emergency_stop` - 紧急停止
- `POST /api/movement/action` - 执行特定动作
- `POST /api/movement/pounce` - 扑跃控制
- `POST /api/movement/manual` - 手动控制

#### 系统状态
- `GET /api/status` - 获取系统整体状态

### WebSocket 事件

#### 服务器发送事件
- `status_update` - 机器狗状态更新
- `movement_update` - 运动状态更新

#### 客户端事件
- `connect` - 客户端连接
- `disconnect` - 客户端断开

## 🎯 使用场景

### 1. 远程演示
- 在Windows中使用Web界面
- WSL后台运行控制服务
- 适合远程演示和展示

### 2. 多人协作
- 多个用户同时访问Web界面
- 实时查看机器狗状态
- 协作控制和监控

### 3. 移动控制
- 响应式设计适配移动设备
- 可以使用手机或平板控制
- 适合户外演示场景

### 4. 开发调试
- 实时API调试
- 状态监控和日志查看
- 快速原型开发

## 🔒 安全说明

### 网络安全
- 默认绑定到 `0.0.0.0:5000`，允许外部访问
- 在生产环境中建议配置防火墙规则
- 可以修改绑定地址限制访问范围

### 访问控制
- 当前版本不包含身份验证
- 建议在受信任网络环境中使用
- 如需安全控制，可添加反向代理

## 🐛 故障排除

### 常见问题

1. **无法访问Web界面**
   - 检查WSL IP地址是否正确
   - 确认端口5000没有被占用
   - 检查防火墙设置

2. **机器狗连接失败**
   - 确认机器狗IP地址正确
   - 检查网络连通性
   - 验证机器狗是否在线

3. **实时状态不更新**
   - 检查WebSocket连接是否正常
   - 刷新页面重新建立连接
   - 检查浏览器兼容性

4. **控制指令无响应**
   - 确认机器狗已连接
   - 检查运动模式是否正确
   - 查看错误消息提示

### 调试方法

1. **检查服务器日志**
   ```bash
   # 查看Web服务器日志
   python web_server.py --debug
   ```

2. **检查浏览器控制台**
   - 打开浏览器开发者工具
   - 查看Console标签页
   - 检查Network标签页的连接状态

3. **网络连接测试**
   ```bash
   # 测试WSL网络连接
   ping <机器狗IP>
   
   # 测试Web服务器端口
   netstat -tlnp | grep :5000
   ```

## 🚀 扩展开发

### 添加新功能

1. **新增API接口**
   - 在 `web_server.py` 中添加路由
   - 实现相应的控制逻辑
   - 更新前端调用

2. **自定义前端组件**
   - 修改 `templates/index.html`
   - 更新 `static/css/style.css`
   - 扩展 `static/js/app.js`

3. **集成新的控制模式**
   - 扩展 `DualDogMovement` 类
   - 添加相应的API接口
   - 更新前端控制界面

### 性能优化

1. **缓存优化**
   - 添加Redis缓存
   - 优化状态更新频率
   - 减少不必要的API调用

2. **并发优化**
   - 使用更高效的异步框架
   - 优化WebSocket连接管理
   - 添加连接池

## 📝 更新日志

### v2.0.0 (当前版本)
- ✅ 完整的Web控制界面
- ✅ 实时状态监控
- ✅ 响应式设计
- ✅ 键盘快捷键支持
- ✅ WebSocket实时通信
- ✅ RESTful API完整实现

### 计划功能
- 🔄 用户身份验证
- 🔄 移动端专用界面
- 🔄 录制和回放功能
- 🔄 多语言支持
- 🔄 数据可视化

---

> 💡 **提示**: 这个Web界面完美解决了tkinter界面过于"远古"的问题，提供了现代化的控制体验，特别适合WSL + Windows的开发环境！